#include <studio.h>

/**
 * main - Entry point
 *
 * Ruturn: Alwaya 0 (Success)
 */
int main(void)
{
        int n;

        for (n = 0; n < 10; n++)
               printf("%d", n);
        putchar(10);       
        return (0);

}
